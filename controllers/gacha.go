package Controllers

import (
	//	"net/http"

	//	Models "techtrain-mission/models"
	//	Utils "techtrain-mission/utils"
	"github.com/gin-gonic/gin"
)

func GetGacha(c *gin.Context) {
	//	token := Utils.ExtractAuthToken(c)

	//	var gacha Models.Gacha
	//	c.BindJSON(&gacha)

	//	times := gacha.Times

	// declare the array here

	// make a for loop and call the GetCharacter N times
	// with the probability

	// keep inserting it one by one and save the list of it as well

	/*  	if err != nil {*/
	/*c.AbortWithStatus(http.StatusNotFound)*/
	/*} else {*/
	/*c.JSON(http.StatusOK, user.Token)*/
	/*}*/

}
